jest.mock("phosphor-react-native/src/icons/Bell", () => {
  const { Text } = require("react-native")

  return Text
})

jest.mock("phosphor-react-native/src/icons/CalendarBlank", () => {
  const { Text } = require("react-native")

  return Text
})

jest.mock("phosphor-react-native/src/icons/CaretRight", () => {
  const { Text } = require("react-native")

  return Text
})

jest.mock("phosphor-react-native/src/icons/Check", () => {
  const { Text } = require("react-native")

  return Text
})

jest.mock("phosphor-react-native/src/icons/Drop", () => {
  const { Text } = require("react-native")

  return Text
})

jest.mock("phosphor-react-native/src/icons/PencilSimple", () => {
  const { Text } = require("react-native")

  return Text
})

jest.mock("phosphor-react-native/src/icons/Plus", () => {
  const { Text } = require("react-native")

  return Text
})

jest.mock("phosphor-react-native/src/icons/Scroll", () => {
  const { Text } = require("react-native")

  return Text
})

jest.mock("phosphor-react-native/src/icons/Trash", () => {
  const { Text } = require("react-native")

  return Text
})

jest.mock("phosphor-react-native/src/icons/Trophy", () => {
  const { Text } = require("react-native")

  return Text
})

jest.mock("phosphor-react-native/src/icons/User", () => {
  const { Text } = require("react-native")

  return Text
})

jest.mock("phosphor-react-native/src/icons/X", () => {
  const { Text } = require("react-native")

  return Text
})
